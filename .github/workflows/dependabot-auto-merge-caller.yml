name: Dependabot auto-merge

on:
  pull_request_target:
    types: [opened, synchronize, reopened, labeled]

permissions:
  contents: write
  pull-requests: write

jobs:
  auto-merge:
    uses: Obsidian-Solutions/.github/.github/workflows/dependabot-auto-merge.yml@c0f4b15212359e6ff4b0bb5e3baa50d5bbe34390
